#include <iostream>

using namespace std;

int main (int argc, char *argv[]) {
  char ascii_char;
  cout << "Please enter a single character: ";
  cin >> ascii_char;
  cout << "Ascii Char = " << int(ascii_char);
  return 0;
}
